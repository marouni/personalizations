[user]
    email = abbass.marouni@gmail.com
    name = Abbass MAROUNI
[alias]
    logc = log --color --graph --pretty=format:'%Cred%h%Creset-%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'--abbrev-commit
    stash-all = stash save --include-untracked
    co = checkout
[status]
    showUntrackedFiles = all
[merge]
    conflictstyle = diff3
[core]
    editor = vim
[color]
    ui = auto
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = red
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = red
    untracked = cyan
[diff]
    tool = meld
[difftool]
    prompt = false
[difftool "meld"]
    cmd = meld "$LOCAL" "$REMOTE"
